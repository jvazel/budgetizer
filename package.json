{
  "name": "budgetizer-root",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "shared",
    "server",
    "client"
  ],
  "scripts": {
    "install-all": "npm install",
    "build": "npm run build --workspace=server",
    "start": "npm start --workspace=server",
    "dev": "concurrently \"npm run dev --workspace=server\" \"npm run dev --workspace=client\"",
    "dev:backend": "npm run dev --workspace=server",
    "dev:frontend": "npm run dev --workspace=client",
    "test": "node -e \"if (process.env.NODE_ENV !== 'production' && !process.env.SKIP_TESTS) { process.exit(require('child_process').spawnSync('npm run test --workspace=server && npm run test --workspace=client', { shell: true, stdio: 'inherit' }).status); } else { console.log('Tests ignorés (environnement de production/déploiement).'); }\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "@hookform/resolvers": "^5.4.0",
    "react-hook-form": "^7.80.0",
    "zod": "^3.24.2"
  }
}
